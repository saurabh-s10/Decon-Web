import 'package:flutter/material.dart';

const Color secondaryColor = Color(0xffd40511);
const Color primaryColor = Color(0xffffcc00);

const Gradient buttonGradient =
    LinearGradient(colors: [primaryColor, Color(0xffFFEC9E)]);
